---
phase: 05-app-electron-portable
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\app\\Inicio\\index.html"
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "12 botones no-piloto tienen clase .disabled, atributo disabled, tooltip 'No disponible en piloto', data-link='#'"
    - "Cards de Stocks/Insumos siguen habilitados para flejes/cajas/cartones/plasticos/recepcion (D-01)"
    - "CSS .btn-opcion.disabled aplica gris + cursor not-allowed + sin hover"
    - "Click handler ignora botones .disabled"
    - "Cards 'Despiece', 'Facturas', 'Ventas' visibles con sus botones disabled (D-03 paridad visual)"
    - "Carpeta web original NO modificada (D-17)"
  artifacts:
    - path: "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\app\\Inicio\\index.html"
      provides: "Menu Inicio forkeado con 12 disabled + tooltip + CSS"
      contains: "class=\"btn-opcion disabled\""
  key_links:
    - from: "Inicio/index.html disabled buttons"
      to: "no-op (data-link='#')"
      via: "click handler con classList.contains('disabled')"
      pattern: "classList\\.contains\\('disabled'\\)"
---

<objective>
Forkear `$BUILD\app\Inicio\index.html` para deshabilitar 12 botones no-piloto con clase CSS, atributo disabled, tooltip. CSS adicional para look gris no clickeable. Click handler bloquea navegacion en disabled.

Purpose: D-03, D-04 — modulos no-piloto visibles pero deshabilitados. Cumple SC-2 (los 6 grupos piloto cargan, los demas no responden).
Output: $BUILD\app\Inicio\index.html con 12 botones marcados (lista exacta de RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-app-electron-portable/05-CONTEXT.md
@.planning/phases/05-app-electron-portable/05-RESEARCH.md
@.planning/phases/05-app-electron-portable/05-03-SUMMARY.md

<interfaces>
## Lista EXACTA de 12 botones a deshabilitar (RESEARCH lineas 773-786)

| # | Card | Boton (texto visible) | data-link original |
|---|------|------------------------|---------------------|
| 1 | Stocks | Verificacion Integridad | `../Verificacion/verificacion.html` |
| 2 | Stocks | Verificacion Madres | `../VerifMadres/VerifMadres.html` |
| 3 | Producción | Producciones Disruptivas | `../Disruptivas/index.html` |
| 4 | Despiece | Despiece x Articulo | `../Despiece/Despiece.html` |
| 5 | Facturas | Lectura de Facturas Entrantes | `../Facturas/index.html` |
| 6 | Facturas | Entrega Proveedores Cervantes | `../Facturas/EntregaProveedoresCervantes.html` |
| 7 | Insumos | Remaches | `#` |
| 8 | Insumos | Bombillas | `#` |
| 9 | Insumos | Garage | `#` |
| 10 | Ventas | Chat Bot Ventas | `../Ventas Chat/index.html` |
| 11 | (top-right) | alertBell -> Alertas | `../Alertas/` |
| 12 | (top-right) | alertInsumos -> Compras | `../Compras/cajas.html` |

## NO deshabilitar (D-01 piloto):
- Cajas, Flejes, Cartones, Partes Plasticas (`../StockFlejes/cajas.html`, `flejes.html`, `cartones.html`, `plasticos.html`) — SI son piloto
- Recepcion Insumos (`../StockFlejes/recepcion.html`) — SI es piloto

## Patron de transformacion por boton

ANTES:
```html
<button class="btn-opcion" data-link="../Verificacion/verificacion.html">Verificacion Integridad</button>
```

DESPUES:
```html
<button class="btn-opcion disabled" title="No disponible en piloto" data-link="#" disabled>Verificacion Integridad</button>
```

## CSS a inyectar en `<style>` existente (al final del bloque)

```css
/* Phase 5: piloto disabled */
.btn-opcion.disabled,
.btn-opcion:disabled {
  background: #e2e8f0 !important;
  color: #94a3b8 !important;
  cursor: not-allowed !important;
  opacity: 0.6;
  border-color: #cbd5e1 !important;
}
.btn-opcion.disabled:hover {
  background: #e2e8f0 !important;
  color: #94a3b8 !important;
  transform: none !important;
  box-shadow: none !important;
}
```

## JS click handler (agregar/reforzar al final del script existente, antes del </script> de cierre)

```javascript
document.querySelectorAll(".btn-opcion").forEach(btn => {
  btn.addEventListener("click", (e) => {
    if (btn.classList.contains('disabled')) {
      e.preventDefault();
      e.stopPropagation();
      return false;
    }
  }, true);
});
```

(NO sobrescribir el handler existente — solo AGREGAR este listener con capture=true para que corra antes que cualquier otro handler de navegacion.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar disabled a 12 botones de Inicio/index.html (fork)</name>
  <files>
    C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\app\Inicio\index.html
  </files>
  <read_first>
    - C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\app\Inicio\index.html (file completo — necesario para encontrar los 12 botones por su data-link y reemplazarlos)
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Lista exhaustiva" lineas 734-794
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Botones no-piloto deshabilitados (Code Examples)" lineas 514-557
  </read_first>
  <action>
    Trabajar SOLO sobre `C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\app\Inicio\index.html`. La carpeta web original (`Z:\...\Gestion Productiva\Inicio\index.html`) NO se toca (D-17).

    **Paso 1**: Para CADA uno de los 12 botones de la tabla `<interfaces>`, hacer Edit con match por `data-link` exacto. Ejemplo para el #1:

    Buscar string que contenga `data-link="../Verificacion/verificacion.html"` y reemplazar el `<button class="btn-opcion"` que lo contiene por:

    ```
    <button class="btn-opcion disabled" title="No disponible en piloto" data-link="#" disabled
    ```

    (Edit tool, old_string = match exacto del button completo, new_string = version disabled. Preservar resto del HTML del boton — texto interno y `</button>`.)

    Repetir para los 12 `data-link` listados. Para los items 7-9 (Remaches/Bombillas/Garage) que YA tienen `data-link="#"`, identificarlos por el texto del boton (ej. `>Remaches<`).

    Para items 11-12 (alertBell y alertInsumos) — son `<div>` no `<button>` segun el HTML actual. Identificar por id (`id="alertBell"`, `id="alertInsumos"`) y agregar la clase `disabled`, atributo `title="No disponible en piloto"`, y modificar onclick para que retorne early. Si su markup es distinto a `btn-opcion`, NO forzar la clase `btn-opcion` — usar clase nueva `.disabled-alert` y agregar CSS equivalente, O eliminar el onclick handler.

    **Paso 2**: Inyectar el bloque CSS de `<interfaces>` al final del primer `<style>...</style>` del archivo (antes del `</style>` de cierre).

    **Paso 3**: Inyectar el bloque JS de `<interfaces>` al final del primer `<script>...</script>` que tenga el handler de click de `.btn-opcion`. Si no hay tal script, agregar al final del body un nuevo `<script>` con ese contenido.

    **Paso 4**: Verificar el conteo:

    ```bash
    grep -c 'class="btn-opcion disabled"' "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/app/Inicio/index.html"
    ```

    Esperado: >= 10 (los 10 buttons. Items 11-12 alertBell/alertInsumos pueden no usar `btn-opcion`, contar aparte).

    Para items 11-12, verificar:

    ```bash
    grep -E 'id="alert(Bell|Insumos)".*(disabled|No disponible)' "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/app/Inicio/index.html"
    ```

    Esperado: 2 lineas (una por cada).
  </action>
  <verify>
    <automated>
    bash -c "FILE='/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/app/Inicio/index.html'; CNT=$(grep -c 'class=\"btn-opcion disabled\"' \"$FILE\"); echo \"disabled buttons: $CNT\"; test \"$CNT\" -ge 10 && grep -q 'No disponible en piloto' \"$FILE\" && grep -q 'classList.contains' \"$FILE\" && grep -q '.btn-opcion.disabled' \"$FILE\" && ! grep -q 'data-link=\"../Verificacion/verificacion.html\"[^#]' \"$FILE\""
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="btn-opcion disabled"'` >= 10 (items 1-10 de la lista)
    - Items 11-12 (alertBell, alertInsumos) tienen `disabled` o tooltip "No disponible en piloto"
    - String `No disponible en piloto` aparece en el archivo
    - CSS `.btn-opcion.disabled` presente
    - JS click handler con `classList.contains('disabled')` presente
    - **Botones piloto preservados:** `data-link="../StockFlejes/cajas.html"`, `data-link="../StockFlejes/flejes.html"`, `data-link="../StockFlejes/cartones.html"`, `data-link="../StockFlejes/plasticos.html"`, `data-link="../StockFlejes/recepcion.html"` siguen sin clase `disabled`
    - Web original `Z:\...\Gestion Productiva\Inicio\index.html` mtime sin cambios
  </acceptance_criteria>
  <done>12 botones no-piloto disabled, 5 botones piloto del card "Insumos"/"Stocks" preservados habilitados, CSS + JS handler en su lugar.</done>
</task>

</tasks>

<verification>
- 12 botones no-piloto deshabilitados (10 btn-opcion + 2 alert iconos)
- 5 botones de StockFlejes (cajas/flejes/cartones/plasticos/recepcion) NO deshabilitados
- CSS aplicado, click handler bloquea navegacion
- Web original sin cambios (D-17)
</verification>

<success_criteria>
- SC-2 (precondicion): los 6 grupos piloto siguen clickeables. No-piloto no responden.
- D-03: paridad visual con web (cards visibles, botones gris).
- D-04: fork solo en Portable, web original intacta.
</success_criteria>

<output>
`.planning/phases/05-app-electron-portable/05-05-SUMMARY.md` con: 12 botones modificados (lista verificada), conteo grep, confirmacion 5 botones StockFlejes preservados, mtime web original sin cambios.
</output>
