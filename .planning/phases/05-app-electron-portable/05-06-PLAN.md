---
phase: 05-app-electron-portable
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\dist\\gestion-productiva-portable.exe"
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "electron-builder ejecuta sin errores"
    - "$BUILD/dist/gestion-productiva-portable.exe existe"
    - ".exe tamaño < 250 MB (sanity check)"
    - "asar contiene los 6 grupos piloto + login + Inicio"
    - "asar NO contiene exclusiones (.planning, *.bas, *.xlsx, LOCKS.txt)"
  artifacts:
    - path: "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\dist\\gestion-productiva-portable.exe"
      provides: ".exe portable Windows x64"
  key_links: []
---

<objective>
Ejecutar `npx electron-builder --win portable --x64` y validar que el .exe se genera, tamaño razonable, asar contiene los 6 grupos piloto.

Purpose: SC-1 — generar el .exe.
Output: $BUILD/dist/gestion-productiva-portable.exe + verificacion contenido asar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-app-electron-portable/05-CONTEXT.md
@.planning/phases/05-app-electron-portable/05-RESEARCH.md
@.planning/phases/05-app-electron-portable/05-04-SUMMARY.md
@.planning/phases/05-app-electron-portable/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build .exe con electron-builder</name>
  <files>
    C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\dist\gestion-productiva-portable.exe
  </files>
  <read_first>
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Code Examples / Build commands" lineas 581-585
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Pitfall 1" lineas 414-422 (carpetas con espacios)
  </read_first>
  <action>
    Ejecutar build via Bash tool. Timeout extendido (puede tomar 2-5 min):

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && npx electron-builder --win portable --x64 2>&1 | tail -30
    ```

    Esperar exit 0. El comando descarga binario portable de Electron (cached desde plan 05-01) y empaqueta `app.asar` + runtime.

    Output esperado al final:
    ```
    • building   target=portable file=dist/gestion-productiva-portable.exe arch=x64
    ```

    Validar que el .exe existe:

    ```bash
    ls -la "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/dist/gestion-productiva-portable.exe"
    ```

    Validar tamaño razonable (60-250 MB):

    ```bash
    SIZE=$(stat -c%s "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/dist/gestion-productiva-portable.exe"); echo "size_bytes=$SIZE"; test "$SIZE" -gt 60000000 && test "$SIZE" -lt 250000000 && echo "SIZE OK"
    ```

    Validar contenido asar — debe incluir los 6 grupos piloto:

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && npx asar list dist/win-unpacked/resources/app.asar 2>&1 | head -100
    ```

    Y verificar paths clave:

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && npx asar list dist/win-unpacked/resources/app.asar 2>&1 | grep -E "^/(login\\.html|index\\.html|app/login\\.html|app/Prov Serv|app/Talleristas|app/Prov Art Terminado|app/StockFlejes|app/Produccion|app/Informes|main\\.js|preload\\.js|app/Inicio/index\\.html)" | sort -u
    ```

    Y verificar que exclusiones NO estan:

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && npx asar list dist/win-unpacked/resources/app.asar 2>&1 | grep -E "(\\.planning|\\.bas|\\.xlsx|LOCKS\\.txt|MACRO_ENTREGAS)" | head -5
    ```

    Esperado: salida vacia (no hay matches).

    Si build falla con error de paths con espacios (Pitfall 1), revisar package.json `build.files` — los paths son strings JSON con espacios y eso es legal para minimatch. NO escapar con backslash. Si igual falla, verificar `npm install` aplico el package.json nuevo de plan 05-04 (correr `npm install` y reintentar build).
  </action>
  <verify>
    <automated>
    bash -c "EXE='/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/dist/gestion-productiva-portable.exe'; test -f \"$EXE\" && SIZE=$(stat -c%s \"$EXE\") && echo \"size=$SIZE\" && test \"$SIZE\" -gt 60000000 && test \"$SIZE\" -lt 250000000 && cd '/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local' && npx asar list dist/win-unpacked/resources/app.asar > /tmp/asar-list.txt 2>&1 && grep -q 'main.js' /tmp/asar-list.txt && grep -q 'preload.js' /tmp/asar-list.txt && grep -q 'app/login.html' /tmp/asar-list.txt && grep -q 'app/Inicio/index.html' /tmp/asar-list.txt && grep -q 'app/Prov Serv' /tmp/asar-list.txt && grep -q 'app/Talleristas' /tmp/asar-list.txt && grep -q 'app/Prov Art Terminado' /tmp/asar-list.txt && grep -q 'app/StockFlejes' /tmp/asar-list.txt && grep -q 'app/Produccion' /tmp/asar-list.txt && grep -q 'app/Informes' /tmp/asar-list.txt && ! grep -q '.planning' /tmp/asar-list.txt && ! grep -q '.bas' /tmp/asar-list.txt && ! grep -q '.xlsx' /tmp/asar-list.txt && ! grep -q 'LOCKS.txt' /tmp/asar-list.txt && echo BUILD_OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `$BUILD/dist/gestion-productiva-portable.exe` existe
    - Tamaño entre 60 MB y 250 MB (sanity range)
    - asar contiene: `main.js`, `preload.js`, `app/login.html`, `app/Inicio/index.html`, `app/Prov Serv`, `app/Talleristas`, `app/Prov Art Terminado`, `app/StockFlejes`, `app/Produccion`, `app/Informes`
    - asar NO contiene: `.planning`, `.bas`, `.xlsx`, `LOCKS.txt`
    - electron-builder exit 0
  </acceptance_criteria>
  <done>.exe portable construido, contenido validado via asar list.</done>
</task>

</tasks>

<verification>
- .exe existe y tamaño razonable
- asar incluye los 6 grupos piloto + login + Inicio + main.js + preload.js
- asar NO incluye exclusiones (.planning, .bas, .xlsx, LOCKS.txt)
</verification>

<success_criteria>
SC-1 (parcial): .exe generado, listo para mover a destino en plan 05-07.
</success_criteria>

<output>
`.planning/phases/05-app-electron-portable/05-06-SUMMARY.md` con: tamaño exacto del .exe, output relevante del build, conteo de archivos en asar, confirmacion de exclusiones.
</output>
