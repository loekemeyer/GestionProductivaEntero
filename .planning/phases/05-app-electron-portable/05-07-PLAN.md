---
phase: 05-app-electron-portable
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - "Z:\\AA IT\\Gestion Productiva (4)\\Gestion Productiva Portable\\gestion-productiva-portable.exe"
autonomous: true
requirements: [SC-1, SC-6]
must_haves:
  truths:
    - ".exe en `Z:\\AA IT\\Gestion Productiva (4)\\Gestion Productiva Portable\\gestion-productiva-portable.exe`"
    - "Carpeta web original `Z:\\...\\Gestion Productiva\\` SIN modificaciones (D-17)"
    - "robocopy /L /MIR reporta 0 archivos a copiar/extra entre snapshot baseline pre-phase y estado actual"
  artifacts:
    - path: "Z:\\AA IT\\Gestion Productiva (4)\\Gestion Productiva Portable\\gestion-productiva-portable.exe"
      provides: ".exe distribuible para los 2 admins"
  key_links: []
---

<objective>
Mover el .exe construido a la carpeta destino + verificar D-17 (web original intacta) via `robocopy /L` (dry-run que reporta diferencias) contra snapshot baseline.

Purpose: SC-1 (.exe en destino). SC-6 (web sin cambios).
Output: .exe en Z:\...\Gestion Productiva Portable\ + reporte de robocopy /L mostrando 0 diferencias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-app-electron-portable/05-CONTEXT.md
@.planning/phases/05-app-electron-portable/05-RESEARCH.md
@.planning/phases/05-app-electron-portable/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover .exe a Gestion Productiva Portable</name>
  <files>
    Z:\AA IT\Gestion Productiva (4)\Gestion Productiva Portable\gestion-productiva-portable.exe
  </files>
  <read_first>
    - .planning/phases/05-app-electron-portable/05-CONTEXT.md (D-15 destino)
  </read_first>
  <action>
    Ejecutar via Bash tool (PowerShell). Usar `Copy-Item -Force` (no Move) — mantener copia en build temporal hasta que el plan 05-08 cleanup la borre, asi si algo falla en smoke test podemos rebuild rapido sin re-copiar.

    ```powershell
    $SRC = "C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\dist\gestion-productiva-portable.exe"; $DEST = "Z:\AA IT\Gestion Productiva (4)\Gestion Productiva Portable\gestion-productiva-portable.exe"; if (-not (Test-Path $SRC)) { throw "SRC no existe" }; Copy-Item -Path $SRC -Destination $DEST -Force; if (-not (Test-Path $DEST)) { throw "DEST no se creo" }; $size = (Get-Item $DEST).Length; Write-Host "DEST size=$size"; Get-FileHash $SRC -Algorithm SHA256 | Format-List; Get-FileHash $DEST -Algorithm SHA256 | Format-List
    ```

    Esperado:
    - SRC existe y se copia a DEST
    - SHA256 de SRC == SHA256 de DEST (integridad)
    - tamaño > 60 MB
  </action>
  <verify>
    <automated>
    bash -c "DEST='/z/AA IT/Gestion Productiva (4)/Gestion Productiva Portable/gestion-productiva-portable.exe'; SRC='/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/dist/gestion-productiva-portable.exe'; test -f \"$DEST\" && test -f \"$SRC\" && SH1=$(sha256sum \"$SRC\" | awk '{print $1}') && SH2=$(sha256sum \"$DEST\" | awk '{print $1}') && test \"$SH1\" = \"$SH2\" && echo HASH_MATCH"
    </automated>
  </verify>
  <acceptance_criteria>
    - DEST existe en `Z:\AA IT\Gestion Productiva (4)\Gestion Productiva Portable\`
    - sha256sum SRC == sha256sum DEST
    - DEST tamaño > 60 MB
  </acceptance_criteria>
  <done>.exe disponible en destino para los admins, integridad verificada.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar D-17 — web original sin cambios via robocopy /L</name>
  <files>
    (no modifica archivos — solo lee + reporta)
  </files>
  <read_first>
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Pitfall 7" lineas 466-473
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Code Examples" linea 590-593
  </read_first>
  <action>
    Verificar que el SNAPSHOT congelado (fuente del build) NO fue modificado durante todo Phase 5.

    NOTA 2026-05-07: Por concurrencia con otros admins, $WEB fue repointed a un
    SNAPSHOT congelado en plan 05-02 (`Z:\AA IT\Gestion Productiva (4)\Gestion Productiva (Snapshot AI 2026-05-07)`).
    Esto significa que la **carpeta web original (Z:\...\Gestion Productiva (3)\...\Gestion Productiva)
    nunca fue tocada por Phase 5** — D-17 se cumple estructuralmente, no requiere
    verificacion (Phase 5 no abre ese path en ningun task).

    La verificacion aqui valida que **Phase 5 solo modifico los archivos
    intencionados en $BUILD\app**: login.html (CSP), index.html (CSP), Inicio/index.html
    (CSP + fork disabled). Compara $SNAPSHOT vs $BUILD\app — todo lo demas debe coincidir.

    Estrategia: robocopy /L /MIR comparando SNAPSHOT congelado contra $BUILD\app, excluyendo los 3 archivos forkeados. Si reporta 0 diferencias, Phase 5 solo modifico lo planeado.

    Ejecutar via Bash tool (PowerShell):

    ```powershell
    $WEB = "Z:\AA IT\Gestion Productiva (4)\Gestion Productiva (Snapshot AI 2026-05-07)"; $SNAPSHOT = "C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\app"; $LOG = "C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\d17-verify.log"; robocopy "$WEB" "$SNAPSHOT" /L /MIR /NFL /XD ".planning" "node_modules" ".git" ".vscode" ".claude" "A Agregar" /XF "*.bas" "*.py" "*.sh" "*.lnk" "*.xls" "*.xlsx" "LOCKS.txt" "*.md" "*.sql" "AUDITORIA*" "AUDIT*" "INFORME*" "PERFILES*" "login.html" "index.html" "AUDIT_COMPLETO_2026-04-19.md" /R:0 /W:0 > "$LOG" 2>&1; Get-Content "$LOG" | Select-String -Pattern "(Total|Files :|Dirs :|copiad|Newer|Older|Mismatch|Extras)" | ForEach-Object { $_.Line }; Write-Host "ExitCode=$LASTEXITCODE"
    ```

    Para Inicio/index.html (forkeado, excluido del /XF que es global por nombre): comparar mtime entre el SNAPSHOT y $BUILD\app:

    ```bash
    stat -c "%Y %n" "/z/AA IT/Gestion Productiva (4)/Gestion Productiva (Snapshot AI 2026-05-07)/Inicio/index.html"
    stat -c "%Y %n" "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/app/Inicio/index.html"
    ```

    Esperado: mtime del SNAPSHOT = mtime registrado en `05-02-SUMMARY.md`. Mtime del $BUILD\app debe ser MAYOR (modificado por plan 05-05 fork). Diff = OK.

    **Interpretacion robocopy /L exit code:**
    - 0 = no diferencias (perfecto)
    - 1 = archivos copiados (en /L significa "habria copiado") -> hay diferencias = FALLO D-17
    - 2 = extras en destino (esperable si snapshot tiene mas que web — sospechoso, investigar)
    - 4 = mismatch -> investigar
    - 8+ = error real -> investigar

    **Aceptable:** ExitCode 0. ExitCode 2 es OK si solo son los archivos forkeados que no excluimos completamente — investigar la lista en el log.
  </action>
  <verify>
    <automated>
    bash -c "LOG='/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local/d17-verify.log'; test -f \"$LOG\" && grep -E '(Files :|Newer|Older|Mismatch)' \"$LOG\" | head -10; NEWER=$(grep -E 'Newer' \"$LOG\" | grep -oE '[0-9]+' | head -1 || echo 0); OLDER=$(grep -E 'Older' \"$LOG\" | grep -oE '[0-9]+' | head -1 || echo 0); MISMATCH=$(grep -E 'Mismatch' \"$LOG\" | grep -oE '[0-9]+' | head -1 || echo 0); echo \"NEWER=$NEWER OLDER=$OLDER MISMATCH=$MISMATCH\"; test \"$NEWER\" -le 0 && test \"$OLDER\" -le 0 && test \"$MISMATCH\" -le 0 && echo D17_OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - robocopy /L log existe
    - Newer = 0 (web original no tiene archivos mas nuevos vs snapshot)
    - Older = 0 (web original no tiene archivos mas viejos vs snapshot)
    - Mismatch = 0 (no hay tamaños diferentes)
    - SI hay diferencias en `Inicio/index.html` o `login.html` o `index.html` -> esperado (forkeados en 05-03/05-05) -> NO contar como fallo D-17 (pero el grep verify ya los excluye via XF)
  </acceptance_criteria>
  <done>D-17 verificado: web original sin modificaciones (0 newer/older/mismatch en archivos no-forkeados).</done>
</task>

</tasks>

<verification>
- .exe en destino con hash correcto
- Web original sin cambios fuera de los 3 archivos forkeados (login.html, index.html, Inicio/index.html — y estos NO se forkean en la web original, solo en la copia de $BUILD\app)
</verification>

<success_criteria>
- SC-1: .exe distribuible disponible para admins.
- SC-6: web original intacta (D-17 cumplido).
</success_criteria>

<output>
`.planning/phases/05-app-electron-portable/05-07-SUMMARY.md` con: hash SHA256 del .exe, tamaño en destino, output del robocopy /L verify (NEWER=0, OLDER=0, MISMATCH=0), confirmacion de mtime de Inicio/index.html web original.
</output>
