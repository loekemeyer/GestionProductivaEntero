---
phase: 05-app-electron-portable
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\main.js"
  - "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\preload.js"
  - "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\package.json"
autonomous: true
requirements: [SC-1, SC-3, SC-4]
must_haves:
  truths:
    - "main.js crea BrowserWindow con contextIsolation:true y nodeIntegration:false"
    - "main.js carga app/index.html via loadFile"
    - "preload.js expone solo openExternal via contextBridge (D-08)"
    - "package.json tiene main:main.js, scripts.start, scripts.build, build.win.target=portable, build.files con exclusiones"
    - "node --check OK en main.js y preload.js"
  artifacts:
    - path: "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\main.js"
      provides: "Main process Electron"
      min_lines: 25
    - path: "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\preload.js"
      provides: "ContextBridge minimo (D-08)"
      min_lines: 5
    - path: "C:\\Users\\Logistica1\\Desktop\\Prueba_Apliaciones_en_Local\\package.json"
      provides: "package.json con build.win.target=portable"
      contains: "portable"
  key_links:
    - from: "main.js"
      to: "preload.js"
      via: "webPreferences.preload path.join"
      pattern: "preload.*path\\.join.*preload\\.js"
    - from: "main.js"
      to: "app/index.html"
      via: "mainWindow.loadFile"
      pattern: "loadFile.*app.*index\\.html"
    - from: "package.json build.files"
      to: "main.js + preload.js + app/**"
      via: "electron-builder files glob"
      pattern: "\"app/\\*\\*/\\*\""
---

<objective>
Crear main.js (BrowserWindow + IPC handler), preload.js (contextBridge openExternal), y reescribir package.json del $BUILD con config completa de electron-builder target portable.

Purpose: D-07 (impresion via dialog OS), D-08 (IPC minimo), D-13 (sin firma), D-14 (sin branding custom). Cumple SC-1 (build config), SC-3 (CSP via webPreferences), SC-4 (window.print() funciona).
Output: 3 archivos en $BUILD raiz, listos para build en plan 05-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-app-electron-portable/05-CONTEXT.md
@.planning/phases/05-app-electron-portable/05-RESEARCH.md
@.planning/phases/05-app-electron-portable/05-01-SUMMARY.md

<interfaces>
## main.js exact content (target Pattern 1 RESEARCH lineas 244-272)

```javascript
const { app, BrowserWindow, shell, ipcMain } = require('electron');
const path = require('path');

let mainWindow;

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1400,
    height: 900,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: false
    }
  });
  mainWindow.loadFile(path.join(__dirname, 'app', 'index.html'));
  mainWindow.setMenuBarVisibility(false);
}

app.whenReady().then(createWindow);

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit();
});

app.on('activate', () => {
  if (BrowserWindow.getAllWindows().length === 0) createWindow();
});

ipcMain.handle('open-external', (_evt, url) => shell.openExternal(url));
```

## preload.js exact content (Pattern 2 RESEARCH lineas 277-287)

```javascript
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('gpPortable', {
  openExternal: (url) => ipcRenderer.invoke('open-external', url)
});
```

## package.json exact content (Pattern 3 RESEARCH lineas 291-338)

```json
{
  "name": "gestion-productiva-portable",
  "productName": "Gestion Productiva Portable",
  "version": "1.0.0",
  "description": "Sistema Gestion Productiva Loeke - piloto admins (Electron portable)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win portable --x64"
  },
  "devDependencies": {
    "electron": "^42.0.0",
    "electron-builder": "^26.0.0"
  },
  "build": {
    "appId": "com.loeke.gestionproductiva.portable",
    "productName": "Gestion Productiva Portable",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "app/**/*",
      "!app/.planning/**",
      "!app/node_modules/**",
      "!app/.git/**",
      "!app/.vscode/**",
      "!app/.claude/**",
      "!app/A Agregar/**",
      "!app/LOCKS.txt",
      "!app/*.md",
      "!app/*.bas",
      "!app/*.py",
      "!app/*.sh",
      "!app/*.lnk",
      "!app/*.xls",
      "!app/*.xlsx",
      "!app/*.sql"
    ],
    "asar": true,
    "win": {
      "target": [{ "target": "portable", "arch": ["x64"] }],
      "artifactName": "gestion-productiva-portable.exe"
    },
    "portable": {
      "artifactName": "gestion-productiva-portable.exe",
      "unpackDirName": false
    }
  }
}
```

NOTA: La devDependencies versions deben coincidir con las instaladas por plan 05-01. Si npm install resolvio versiones especificas, las preserva el devDeps existente — usar `^42.0.0` y `^26.0.0` como semver, npm respeta lo que ya tiene. Si conflicto, ejecutar `npm install` despues de escribir package.json para reconciliar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear main.js + preload.js</name>
  <files>
    C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\main.js
    C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\preload.js
  </files>
  <read_first>
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Pattern 1" lineas 241-272
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Pattern 2" lineas 274-287
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Anti-Patterns" lineas 368-380 (porque contextIsolation:true es obligatorio)
  </read_first>
  <action>
    Usar Write tool para crear ambos archivos con el contenido EXACTO mostrado en `<interfaces>` arriba.

    Archivo 1: `C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\main.js` — bloque main.js de interfaces, sin modificaciones.

    Archivo 2: `C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\preload.js` — bloque preload.js de interfaces, sin modificaciones.

    NO agregar `webSecurity: false`. NO habilitar `nodeIntegration: true`. NO exponer `fs` ni `child_process` al renderer. D-08 es estricto.

    Despues correr node --check para validar sintaxis:

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && node --check main.js && node --check preload.js && echo "SYNTAX OK"
    ```
  </action>
  <verify>
    <automated>
    bash -c "cd '/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local' && node --check main.js && node --check preload.js && grep -q 'contextIsolation: true' main.js && grep -q 'nodeIntegration: false' main.js && grep -q \"loadFile.*app.*index\\.html\" main.js && grep -q 'contextBridge.exposeInMainWorld' preload.js && grep -q 'openExternal' preload.js && ! grep -q 'webSecurity: false' main.js && ! grep -q 'nodeIntegration: true' main.js"
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --check main.js` exit 0
    - `node --check preload.js` exit 0
    - main.js contiene `contextIsolation: true`
    - main.js contiene `nodeIntegration: false`
    - main.js contiene `loadFile(...app...index.html)` (regex)
    - main.js contiene `ipcMain.handle('open-external'`
    - preload.js contiene `contextBridge.exposeInMainWorld`
    - preload.js contiene `openExternal`
    - main.js NO contiene `webSecurity: false`
    - main.js NO contiene `nodeIntegration: true`
  </acceptance_criteria>
  <done>main.js + preload.js validos sintacticamente, security flags correctos.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir package.json con config electron-builder portable</name>
  <files>
    C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\package.json
  </files>
  <read_first>
    - $BUILD\package.json (estado actual post npm install — leer para confirmar versiones de electron/electron-builder resueltas)
    - .planning/phases/05-app-electron-portable/05-RESEARCH.md §"Pattern 3" lineas 289-338
    - .planning/phases/05-app-electron-portable/05-CONTEXT.md §"specifics" (naming default `gestion-productiva-portable.exe`)
  </read_first>
  <action>
    Usar Write tool para sobrescribir `C:\Users\Logistica1\Desktop\Prueba_Apliaciones_en_Local\package.json` con el bloque package.json completo mostrado en `<interfaces>` arriba.

    Despues correr `npm install` para reconciliar (en caso de cambios):

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && npm install --silent && echo "RECONCILED"
    ```

    Validar JSON parseable:

    ```bash
    cd "/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local" && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('JSON OK')"
    ```
  </action>
  <verify>
    <automated>
    bash -c "cd '/c/Users/Logistica1/Desktop/Prueba_Apliaciones_en_Local' && node -e \"const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(p.main!=='main.js')throw 'main'; if(!p.scripts.build.includes('portable'))throw 'build script'; if(!p.build)throw 'build cfg'; if(p.build.win.target[0].target!=='portable')throw 'target'; if(p.build.win.target[0].arch[0]!=='x64')throw 'arch'; if(!p.build.files.includes('main.js'))throw 'files main'; if(!p.build.files.includes('preload.js'))throw 'files preload'; if(!p.build.files.includes('app/**/*'))throw 'files app'; if(!p.build.portable||p.build.portable.artifactName!=='gestion-productiva-portable.exe')throw 'artifactName'; console.log('PKG OK');\""
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parseable como JSON valido
    - `main` = `main.js`
    - `scripts.build` contiene `portable`
    - `build.win.target[0].target` = `portable`
    - `build.win.target[0].arch[0]` = `x64`
    - `build.files` incluye `main.js`, `preload.js`, `app/**/*` y exclusiones (.planning, .git, *.bas, *.py, *.xlsx, LOCKS.txt, etc)
    - `build.portable.artifactName` = `gestion-productiva-portable.exe`
    - `build.appId` = `com.loeke.gestionproductiva.portable`
  </acceptance_criteria>
  <done>package.json completo y valido, listo para `npx electron-builder --win portable --x64`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer -> main (IPC) | Solo expone openExternal con URLs (sin user input — uso interno) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | E (Elevation) | preload bridge | mitigate | `contextIsolation: true`, solo `openExternal` expuesto. NO `fs`, NO `child_process`, NO `require`. (D-08) |
| T-05-05 | T (Tampering) | shell.openExternal | accept | URLs hardcodeadas en codigo (no user input). Para piloto OK. |
| T-05-06 | S (Spoofing) | URL desconocida en openExternal | accept | Renderer solo invoca con paths file:// internos. Bajo riesgo. |
</threat_model>

<verification>
- main.js + preload.js sintaxis OK (`node --check`)
- security flags correctos (contextIsolation true, nodeIntegration false)
- package.json electron-builder config completa
- artifactName = gestion-productiva-portable.exe
</verification>

<success_criteria>
- SC-1 (precondicion): build config completa.
- SC-3: CSP via webPreferences default + meta tag CSP del plan 05-03.
- SC-4: window.print() funciona out-of-box (no requiere config en main.js, dialog OS via Chromium).
- D-08: IPC minimo (solo openExternal).
- D-13/D-14: sin firma, sin branding (defaults electron-builder).
</success_criteria>

<output>
`.planning/phases/05-app-electron-portable/05-04-SUMMARY.md` con: 3 archivos creados, output `node --check`, output verify package.json, decisiones tomadas (sandbox:false, setMenuBarVisibility para hide menu).
</output>
